{
  "name": "warriorbeatapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "yarn run pack && sls deploy",
    "pack": "rm requirements.txt && pipenv run pip freeze > requirements.txt",
    "up": "yarn run pack && sls deploy function -f app",
    "startdb": "sls dynamodb start",
    "local": "sls wsgi serve",
    "detach_local": "yarn run pack && (sls dynamodb start &) && (sls wsgi serve &)",
    "dlo": "yarn run detach_local"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "js-yaml": "^3.9.1",
    "serverless-dynamodb-local": "^0.2.33",
    "serverless-python-requirements": "^2.5.0",
    "serverless-wsgi": "^1.3.0"
  }
}
